-- El cron de daily-analysis se configura desde el Dashboard de Supabase:
-- Edge Functions → daily-analysis → Schedule → Add schedule → "0 8 * * *"
-- Este archivo existe como registro. No ejecutar manualmente.
SELECT 1;
